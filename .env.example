# AKTREA LMS Refactor 2.0 - Environment Configuration
# Copy this file to .env and modify the values as needed for your environment.
# This file contains sensitive information and should never be committed to Git.

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# MariaDB Database Settings
DB_DATABASE=akt_lms
DB_USER=lms_user
DB_PASSWORD=lms_password
DB_ROOT_PASSWORD=akt_lms_password

# =============================================================================
# WEB SERVER CONFIGURATION
# =============================================================================
# Port on which the AKTREA LMS will be accessible
# Access your LMS at: http://localhost:8080
WEB_PORT=8080

# =============================================================================
# PHPMYADMIN CONFIGURATION
# =============================================================================
# Port for accessing phpMyAdmin database management interface
# Access phpMyAdmin at: http://localhost:8081
PHPMYADMIN_PORT=8081

# =============================================================================
# SECURITY NOTES
# =============================================================================
# 1. Change all default passwords to strong, unique values
# 2. For production deployments, use environment-specific values
# 3. Never commit the actual .env file to version control
# 4. Keep this file secure and limit access permissions

# =============================================================================
# MOODLE/IOMAD INSTALLATION NOTES
# =============================================================================
# During the Moodle installation wizard, use these database settings:
# - Database type: MariaDB (native/mysqli)
# - Database host: db (this is the Docker container name)
# - Database name: Use the DB_DATABASE value above
# - Database user: Use the DB_USER value above  
# - Database password: Use the DB_PASSWORD value above
# - Database port: 3306 (default MariaDB port)